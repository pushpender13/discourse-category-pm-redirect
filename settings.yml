restricted_categories:
  default: ""
  type: list

pm_target:
  default: "moderators"
  type: string

use_group:
  default: true
  type: bool

pm_fallback_target:
  default: "moderators"
  type: string
