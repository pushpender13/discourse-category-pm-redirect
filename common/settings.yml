pm_target:
  default: "moderators"
  type: string
  description: "Username or group name to receive join requests"

use_group:
  default: true
  type: bool
  description: "If true, sends PM to a group; if false, sends PM to a specific username"
